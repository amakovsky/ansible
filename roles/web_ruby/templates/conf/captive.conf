server {
  listen 185.9.87.23:80;
  server_name hotspot.hot-spot.by;
  
  # Optional: SSL
  #listen 10.1.240.227:443 ssl;
  
  #ssl on;
  #ssl_certificate /opt/nginx/cert/cert.crt;
  #ssl_certificate_key /opt/nginx/cert/cert.key;
  
  # Cache sessions
  #ssl_session_cache shared:SSL:50m;
  #ssl_session_timeout 180m;
  
  # Use modern SSL (Bye-bye IE6)
  #ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
  #ssl_prefer_server_ciphers on;
  #ssl_ciphers ECDH+AESGCM:ECDH+AES256:ECDH+AES128:DH+3DES:!ADH:!AECDH:!MD5;
  #ssl_dhparam /opt/nginx/cert/dhparam2.pem;
 
  root /opt/www/captive;
  access_log  logs/captive.log  main; 
  
  location / {
    try_files $uri /index.html;
  }

}
